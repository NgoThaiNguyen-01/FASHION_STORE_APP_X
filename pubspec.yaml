name: fashion_store_app_x
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  sqflite: ^2.4.2
  path: ^1.9.1
  bcrypt: ^1.1.3
  image_picker: ^1.1.2
  intl: ^0.19.0  # THÊM PACKAGE NÀY ĐỂ ĐỊNH DẠNG TIỀN VNĐ
  file_picker: ^8.0.0
  path_provider: ^2.1.3
  shared_preferences: ^2.2.2
  flutter_rating_bar: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # =========================
  # 📁 Thêm tất cả file ảnh & video
  # =========================
  assets:
    - assets/images/
    - assets/videos/